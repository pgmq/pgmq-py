{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"PGMQ Python Client Documentation","text":"<p>Welcome to the official documentation for <code>pgmq</code>, the Python client library for the PGMQ PostgreSQL extension.</p>"},{"location":"#what-is-pgmq","title":"What is PGMQ?","text":"<p>A lightweight message queue. Like AWS SQS and RSMQ but on Postgres.</p> <p>PGMQ (Postgres Message Queue) is a message queue built on Postgres. It provides reliable, transactional message processing with the familiarity of SQL. The <code>pgmq</code> Python library exposes a clean, unified API for interacting with PGMQ across four different database backends.</p>"},{"location":"#key-features","title":"Key Features","text":"<ul> <li>Four Identical APIs: Swap between sync (<code>psycopg</code>), async (<code>asyncpg</code>), sync SQLAlchemy, and async SQLAlchemy with minimal code changes.</li> <li>Queue Management: Create, drop, list, and purge queues. Support for partitioned and unlogged queues.</li> <li>Message Operations: Send, read, archive, delete, and pop messages. Batch operations for high throughput.</li> <li>Topic Routing: Route messages to multiple queues using pattern-based bindings.</li> <li>FIFO Support: Grouped and round-robin reads for ordered processing.</li> <li>Notifications: Built-in PostgreSQL <code>NOTIFY</code>/<code>LISTEN</code> support for real-time message arrival events.</li> <li>Transactions: First-class decorators and manual connection injection for complex transactional workflows.</li> <li>Flexible Configuration: Environment variables, connection strings, or explicit parameters.</li> <li>Structured Logging: stdlib <code>logging</code> with optional <code>loguru</code> backend, rotation, and structured JSON output.</li> </ul>"},{"location":"#documentation-structure","title":"Documentation Structure","text":"Document Description Getting Started Installation, Docker setup, and your first message. Configuration <code>PGMQConfig</code>, connection strings, and environment variables. Clients Choosing and initializing the four client backends. Queue Management Creating, dropping, listing, and partitioning queues. Messages Dataclasses: <code>Message</code>, <code>QueueRecord</code>, <code>QueueMetrics</code>, and more. Sending Messages <code>send</code>, <code>send_batch</code>, headers, and delayed delivery. Reading Messages <code>read</code>, <code>read_with_poll</code>, conditional reads, and FIFO variants. Deleting and Archiving <code>delete</code>, <code>archive</code>, <code>pop</code>, and <code>purge</code>. Visibility Timeout Extending or changing message visibility with <code>set_vt</code>. Topic Routing Pattern-based message routing across queues. Metrics Queue statistics and monitoring. Notifications Real-time notifications and listeners. Transactions Transaction decorators and manual connection passing. Logging Configuring loggers, structured output, and performance tracking. Utilities Validation helpers, FIFO indexes, and archive conversion. Backward Compatibility Migration notes and legacy behavior. Development Running tests and contributing to the library."},{"location":"#quick-example","title":"Quick Example","text":"<pre><code>from pgmq import PGMQueue\n\nqueue = PGMQueue()\nqueue.create_queue(\"my_queue\")\nmsg_id = queue.send(\"my_queue\", {\"hello\": \"world\"})\nmsg = queue.read(\"my_queue\", vt=30)\nprint(msg.message)  # {'hello': 'world'}\n</code></pre> <p>For async usage:</p> <pre><code>from pgmq import AsyncPGMQueue\n\nqueue = AsyncPGMQueue()\nawait queue.init()\nawait queue.create_queue(\"my_queue\")\nmsg_id = await queue.send(\"my_queue\", {\"hello\": \"world\"})\n</code></pre>"},{"location":"#backward-compatibility","title":"Backward Compatibility","text":"<p>This library is actively developed while maintaining strong backward compatibility. See Backward Compatibility for details on deprecated aliases, behavior changes, and migration paths.</p>"},{"location":"backward_compatibility/","title":"Backward Compatibility","text":"<p>The <code>pgmq</code> library is actively developed while maintaining strong backward compatibility. This document tracks known aliases, deprecations, and behavior changes.</p>"},{"location":"backward_compatibility/#import-aliases","title":"Import Aliases","text":"Alias Points To Status <code>PGMQueue</code> <code>SyncPGMQueue</code> (psycopg sync) Stable alias <code>SyncPGMQueue</code> <code>pgmq.queue.PGMQueue</code> Explicit sync name <code>AsyncPGMQueue</code> <code>pgmq.async_queue.PGMQueue</code> Explicit async name <code>SQLAlchemyPGMQueue</code> <code>pgmq.sqlalchemy_queue.PGMQueue</code> Explicit sync SQLAlchemy name <code>SQLAlchemyAsyncPGMQueue</code> <code>pgmq.sqlalchemy_async_queue.PGMQueue</code> Explicit async SQLAlchemy name <p>All aliases are exported from <code>pgmq</code> top-level package.</p>"},{"location":"backward_compatibility/#parameter-aliases","title":"Parameter Aliases","text":""},{"location":"backward_compatibility/#tz-delay","title":"<code>tz</code> \u2192 <code>delay</code>","text":"<p>The <code>send()</code> method accepts both <code>delay</code> and <code>tz</code> for specifying message delay. <code>tz</code> is a backward-compatible alias:</p> <pre><code>queue.send(\"q\", {\"a\": 1}, delay=60)\nqueue.send(\"q\", {\"a\": 1}, tz=60)      # identical behavior\nqueue.send(\"q\", {\"a\": 1}, delay=60, tz=30)  # tz wins if both provided\n</code></pre>"},{"location":"backward_compatibility/#list_queues-return-type","title":"<code>list_queues()</code> Return Type","text":"<p><code>list_queues()</code> now returns <code>List[QueueRecord]</code> instead of <code>List[str]</code>.</p> <pre><code>queues = queue.list_queues()\nfor q in queues:\n    print(q.queue_name)  # access the string name\n</code></pre> <p>A <code>UserWarning</code> is emitted on the sync client to alert callers of the change.</p>"},{"location":"backward_compatibility/#read_batch-alias","title":"<code>read_batch()</code> Alias","text":"<p><code>read_batch(queue, vt=30, batch_size=5)</code> is a convenience wrapper around <code>read(queue, vt=30, qty=5)</code> that always returns a list.</p>"},{"location":"backward_compatibility/#detach-archive","title":"Detach Archive","text":"<p><code>detach_archive()</code> is deprecated in PGMQ v2.0 but remains available in the client for compatibility with older extension versions.</p>"},{"location":"backward_compatibility/#sqlalchemy-client-initialization","title":"SQLAlchemy Client Initialization","text":"<p>Sync SQLAlchemy clients initialize automatically in <code>__post_init__</code>, similar to the psycopg client. Async SQLAlchemy clients require <code>await queue.init()</code>, consistent with the asyncpg client.</p>"},{"location":"backward_compatibility/#version-synchronization","title":"Version Synchronization","text":"<p><code>pyproject.toml</code> declares the version. When releasing, ensure it is updated; <code>src/pgmq/__init__.py</code> will fetch the new version automatically.</p>"},{"location":"clients/","title":"Clients","text":"<p><code>pgmq</code> provides four client implementations with identical public method signatures. This design lets you swap backends with minimal code changes.</p>"},{"location":"clients/#client-overview","title":"Client Overview","text":"Import Alias Module Backend Transaction Decorator <code>PGMQueue</code> <code>pgmq.queue</code> <code>psycopg</code> + <code>ConnectionPool</code> <code>@transaction</code> <code>SyncPGMQueue</code> <code>pgmq.queue</code> <code>psycopg</code> + <code>ConnectionPool</code> <code>@transaction</code> <code>AsyncPGMQueue</code> <code>pgmq.async_queue</code> <code>asyncpg</code> + <code>Pool</code> <code>@async_transaction</code> <code>SQLAlchemyPGMQueue</code> <code>pgmq.sqlalchemy_queue</code> SQLAlchemy <code>Engine</code> <code>@sqlalchemy_transaction</code> <code>SQLAlchemyAsyncPGMQueue</code> <code>pgmq.sqlalchemy_async_queue</code> SQLAlchemy <code>AsyncEngine</code> <code>@sqlalchemy_async_transaction</code> <p>Backward compatibility: <code>PGMQueue</code> is aliased to the sync <code>psycopg</code> version by default.</p>"},{"location":"clients/#sync-psycopg-client","title":"Sync psycopg Client","text":"<p>Auto-initializes in <code>__post_init__</code>. No explicit <code>init()</code> call is required.</p> <pre><code>from pgmq import PGMQueue  # or SyncPGMQueue\n\nqueue = PGMQueue()\nqueue.create_queue(\"my_queue\")\n</code></pre>"},{"location":"clients/#using-an-external-connection-pool","title":"Using an External Connection Pool","text":"<p>You can provide your own <code>psycopg_pool.ConnectionPool</code> instead of letting the client create one. This is useful when you want to share a pool across multiple queue instances or with other parts of your application.</p> <pre><code>from pgmq import PGMQueue\nfrom psycopg_pool import ConnectionPool\n\npool = ConnectionPool(\"host=localhost dbname=postgres user=postgres\", open=True)\nqueue = PGMQueue(pool=pool)\n\nqueue.create_queue(\"my_queue\")\n\n# The queue will NOT close a user-provided pool on shutdown.\n# You are responsible for closing it yourself when appropriate.\npool.close()\n</code></pre>"},{"location":"clients/#async-asyncpg-client","title":"Async asyncpg Client","text":"<p>Requires an explicit <code>await queue.init()</code> before use. Remember to <code>await queue.close()</code> on shutdown.</p> <pre><code>from pgmq import AsyncPGMQueue\n\nqueue = AsyncPGMQueue()\nawait queue.init()\n\nawait queue.create_queue(\"my_queue\")\nmsg_id = await queue.send(\"my_queue\", {\"hello\": \"world\"})\n\nawait queue.close()\n</code></pre>"},{"location":"clients/#using-an-external-connection-pool_1","title":"Using an External Connection Pool","text":"<p>You can inject an existing <code>asyncpg.Pool</code>. The queue will use it for all operations but will not close it when <code>queue.close()</code> is called.</p> <pre><code>from pgmq import AsyncPGMQueue\nimport asyncpg\n\npool = await asyncpg.create_pool(\"postgresql://postgres:postgres@localhost/postgres\")\nqueue = AsyncPGMQueue(pool=pool)\nawait queue.init()\n\nawait queue.create_queue(\"my_queue\")\n\n# queue.close() is safe \u2014 it will leave your external pool open.\nawait queue.close()\n\n# Close the pool yourself when the application shuts down.\nawait pool.close()\n</code></pre>"},{"location":"clients/#sync-sqlalchemy-client","title":"Sync SQLAlchemy Client","text":"<p>Uses SQLAlchemy Core. You can pass an existing <code>Engine</code> or let the client build one from connection parameters.</p> <pre><code>from pgmq import SQLAlchemyPGMQueue\nfrom sqlalchemy import create_engine\n\n# From parameters\nqueue = SQLAlchemyPGMQueue(\n    host=\"localhost\",\n    port=\"5432\",\n    username=\"postgres\",\n    password=\"postgres\",\n    database=\"postgres\",\n)\n\n# Or with an existing engine\nengine = create_engine(\"postgresql+psycopg://...\")\nqueue = SQLAlchemyPGMQueue(engine=engine)\n\nqueue.create_queue(\"my_queue\")\n</code></pre> <p>You can also obtain an ORM <code>Session</code> from the queue:</p> <pre><code>session = queue.session()\n</code></pre>"},{"location":"clients/#async-sqlalchemy-client","title":"Async SQLAlchemy Client","text":"<p>Requires <code>await queue.init()</code> like the asyncpg client.</p> <pre><code>from pgmq import SQLAlchemyAsyncPGMQueue\n\nqueue = SQLAlchemyAsyncPGMQueue(\n    host=\"localhost\",\n    port=\"5432\",\n    username=\"postgres\",\n    password=\"postgres\",\n    database=\"postgres\",\n)\nawait queue.init()\n\nawait queue.create_queue(\"my_queue\")\n\n# AsyncSession support\nasync with queue.session() as session:\n    ...\n\nawait queue.close()\n</code></pre>"},{"location":"clients/#choosing-a-backend","title":"Choosing a Backend","text":"Use Case Recommended Client Simple sync scripts <code>PGMQueue</code> (psycopg) High-throughput async services <code>AsyncPGMQueue</code> (asyncpg) Existing SQLAlchemy application <code>SQLAlchemyPGMQueue</code> or <code>SQLAlchemyAsyncPGMQueue</code> Need both PGMQ + ORM in one transaction SQLAlchemy variant"},{"location":"clients/#unified-api","title":"Unified API","text":"<p>Regardless of backend, all clients expose the same methods:</p> <pre><code># Queue management\n.create_queue(name)\n.create_partitioned_queue(name, ...)\n.drop_queue(name)\n.list_queues()\n\n# Messaging\n.send(queue, msg)\n.send_batch(queue, msgs)\n.read(queue, vt=30)\n.read_with_poll(queue, vt=30, qty=1, ...)\n.pop(queue)\n\n# Lifecycle\n.delete(queue, msg_id)\n.archive(queue, msg_id)\n.purge(queue)\n\n# And more...\n</code></pre> <p>See the remaining documentation sections for detailed usage of each feature.</p>"},{"location":"configuration/","title":"Configuration","text":"<p>All PGMQ clients share a common configuration layer centered around the <code>PGMQConfig</code> dataclass. You can provide settings via environment variables, a connection string, explicit keyword arguments, or a combination of all three.</p>"},{"location":"configuration/#priority-of-configuration-sources","title":"Priority of Configuration Sources","text":"<ol> <li>Explicit keyword arguments passed to the client constructor.</li> <li>Environment variables (<code>PG_HOST</code>, <code>DATABASE_URL</code>, etc.).</li> <li>Built-in defaults (<code>localhost</code>, <code>5432</code>, <code>postgres</code>, etc.).</li> </ol>"},{"location":"configuration/#environment-variables","title":"Environment Variables","text":"Variable Purpose Default <code>PG_HOST</code> Database host <code>localhost</code> <code>PG_PORT</code> Database port <code>5432</code> <code>PG_DATABASE</code> Database name <code>postgres</code> <code>PG_USERNAME</code> Username <code>postgres</code> <code>PG_PASSWORD</code> Password <code>postgres</code> <code>DATABASE_URL</code> Full connection string (URI or libpq) <code>None</code>"},{"location":"configuration/#using-a-connection-string","title":"Using a Connection String","text":"<p>You can pass a full connection string via the <code>conn_string</code> argument or the <code>DATABASE_URL</code> environment variable. Both URI and libpq formats are supported.</p>"},{"location":"configuration/#uri-format","title":"URI Format","text":"<pre><code>queue = PGMQueue(conn_string=\"postgresql://user:pass@host:5432/dbname\")\n</code></pre>"},{"location":"configuration/#libpq-format","title":"Libpq Format","text":"<pre><code>queue = PGMQueue(conn_string=\"host=localhost port=5432 dbname=mydb user=postgres password=secret\")\n</code></pre> <p>When <code>conn_string</code> is provided, individual fields (<code>host</code>, <code>port</code>, etc.) are automatically populated from it. You can still override specific fields by passing them explicitly.</p>"},{"location":"configuration/#explicit-configuration","title":"Explicit Configuration","text":"<pre><code>from pgmq import PGMQueue\n\nqueue = PGMQueue(\n    host=\"0.0.0.0\",\n    port=\"5432\",\n    username=\"postgres\",\n    password=\"postgres\",\n    database=\"postgres\",\n    pool_size=20,\n    verbose=True,\n    log_filename=\"pgmq.log\",\n)\n</code></pre>"},{"location":"configuration/#configuration-options-reference","title":"Configuration Options Reference","text":"Option Type Default Description <code>conn_string</code> <code>str \\| None</code> <code>DATABASE_URL</code> or <code>None</code> Full PostgreSQL connection string. Hidden from <code>repr</code>. <code>host</code> <code>str</code> <code>localhost</code> Database host. <code>port</code> <code>str</code> <code>5432</code> Database port. <code>database</code> <code>str</code> <code>postgres</code> Database name. <code>username</code> <code>str</code> <code>postgres</code> Login username. <code>password</code> <code>str</code> <code>postgres</code> Login password. <code>delay</code> <code>int</code> <code>0</code> Default message delay (seconds). <code>vt</code> <code>int</code> <code>30</code> Default visibility timeout (seconds). <code>pool_size</code> <code>int</code> <code>10</code> Connection pool size. <code>verbose</code> <code>bool</code> <code>False</code> Enable <code>DEBUG</code> level logging. <code>log_filename</code> <code>str \\| None</code> <code>None</code> Optional log file path. <code>structured_logging</code> <code>bool</code> <code>False</code> Output logs as JSON. <code>log_rotation</code> <code>bool</code> <code>False</code> Enable log file rotation. <code>log_rotation_size</code> <code>str</code> <code>10 MB</code> Rotation threshold (loguru format). <code>log_retention</code> <code>str</code> <code>1 week</code> Log retention period (loguru format). <code>init_extension</code> <code>bool</code> <code>True</code> Automatically run <code>CREATE EXTENSION IF NOT EXISTS pgmq</code>."},{"location":"configuration/#reusing-a-config-object","title":"Reusing a Config Object","text":"<pre><code>from pgmq.base import PGMQConfig\nfrom pgmq import PGMQueue, AsyncPGMQueue\n\nconfig = PGMQConfig(\n    host=\"pg.example.com\",\n    port=\"5432\",\n    database=\"app\",\n    username=\"app_user\",\n    password=\"secret\",\n    verbose=True,\n)\n\nsync_queue = PGMQueue(config=config)\nasync_queue = AsyncPGMQueue(config=config)\nawait async_queue.init()\n</code></pre>"},{"location":"configuration/#security-note","title":"Security Note","text":"<p><code>PGMQConfig.conn_string</code> uses <code>repr=False</code> to prevent credentials from leaking into logs or tracebacks when the config object is printed.</p>"},{"location":"deleting_and_archiving/","title":"Deleting and Archiving","text":""},{"location":"deleting_and_archiving/#archive-a-message","title":"Archive a Message","text":"<p>Archiving moves a message from the queue to the archive table. Archived messages are retained for auditing but are no longer visible to readers.</p> <pre><code>archived: bool = queue.archive(\"my_queue\", msg.msg_id)\n</code></pre>"},{"location":"deleting_and_archiving/#archive-a-batch","title":"Archive a Batch","text":"<pre><code>archived_ids: list[int] = queue.archive_batch(\"my_queue\", [msg_id_1, msg_id_2])\n</code></pre>"},{"location":"deleting_and_archiving/#delete-a-message","title":"Delete a Message","text":"<p>Deleting permanently removes a message. It cannot be recovered.</p> <pre><code>deleted: bool = queue.delete(\"my_queue\", msg.msg_id)\n</code></pre>"},{"location":"deleting_and_archiving/#delete-a-batch","title":"Delete a Batch","text":"<pre><code>deleted_ids: list[int] = queue.delete_batch(\"my_queue\", [msg_id_1, msg_id_2])\n</code></pre>"},{"location":"deleting_and_archiving/#pop-a-message","title":"Pop a Message","text":"<p><code>pop</code> reads and immediately deletes a message in a single atomic operation. This is useful when you want to process a message exactly once without worrying about visibility timeouts.</p> <pre><code>msg = queue.pop(\"my_queue\")\nprint(msg.message)\n</code></pre> <p>You can also pop multiple messages at once:</p> <pre><code>msgs = queue.pop(\"my_queue\", qty=5)\n</code></pre>"},{"location":"deleting_and_archiving/#return-type","title":"Return Type","text":"<ul> <li>If <code>qty == 1</code>, returns <code>Message | None</code>.</li> <li>If <code>qty &gt; 1</code>, returns <code>list[Message]</code>.</li> </ul>"},{"location":"deleting_and_archiving/#purge-a-queue","title":"Purge a Queue","text":"<p>Remove all messages from a queue without dropping the queue itself:</p> <pre><code>purged_count: int = queue.purge(\"my_queue\")\n</code></pre>"},{"location":"deleting_and_archiving/#detach-archive","title":"Detach Archive","text":"<p>Detach the archive table from PGMQ management. This is deprecated in PGMQ v2.0 but retained for backward compatibility.</p> <pre><code>queue.detach_archive(\"my_queue\")\n</code></pre>"},{"location":"deleting_and_archiving/#convert-archive-to-partitioned","title":"Convert Archive to Partitioned","text":"<p>If your archive table has grown very large, you can convert it to a partitioned table:</p> <pre><code>queue.convert_archive_partitioned(\n    \"my_queue\",\n    partition_interval=10000,\n    retention_interval=100000,\n    leading_partition=10,\n)\n</code></pre> Parameter Description <code>partition_interval</code> Rows per partition. <code>retention_interval</code> Retained partitions. <code>leading_partition</code> Number of future partitions to create."},{"location":"development/","title":"Development","text":""},{"location":"development/#project-setup","title":"Project Setup","text":"<p>This project uses <code>uv</code> as its build system and package manager.</p> <pre><code># Install everything (dev + all extras + bench)\nuv sync --all-groups --all-extras\n</code></pre>"},{"location":"development/#running-tests","title":"Running Tests","text":"<p>Tests require a running PostgreSQL instance with the PGMQ extension.</p>"},{"location":"development/#automated-docker","title":"Automated (Docker)","text":"<pre><code>make test\n</code></pre> <p>This command: 1. Tears down any existing <code>pgmq-postgres</code> container. 2. Starts a fresh PGMQ-enabled Postgres container. 3. Waits for it to be ready. 4. Runs the full test suite.</p>"},{"location":"development/#manual","title":"Manual","text":"<p>If you already have PGMQ installed:</p> <pre><code>uv run python -m unittest discover -s tests -p \"test_*.py\"\n</code></pre> <p>Override connection parameters with environment variables:</p> <pre><code>export PG_HOST=localhost\nexport PG_PORT=5432\nexport PG_USERNAME=postgres\nexport PG_PASSWORD=postgres\nexport PG_DATABASE=postgres\n</code></pre>"},{"location":"development/#docker-helpers","title":"Docker Helpers","text":"<pre><code># Start a local PGMQ-enabled Postgres\nmake run-pgmq-postgres\n\n# Tear it down\nmake clear-postgres\n</code></pre>"},{"location":"development/#lint-and-format","title":"Lint and Format","text":"<pre><code># Auto-fix and format\nmake format\n\n# Check only\nmake lint\n</code></pre> <p>These wrap <code>ruff</code> \u2014 the only linter/formatter used in this project.</p>"},{"location":"development/#writing-tests","title":"Writing Tests","text":"<ul> <li>Sync + psycopg tests go in <code>tests/test_integration.py</code>.</li> <li>Async + asyncpg tests go in <code>tests/test_async_integration.py</code>.</li> <li>SQLAlchemy sync tests go in <code>tests/test_sqlalchemy_integration.py</code>.</li> <li>SQLAlchemy async tests go in <code>tests/test_sqlalchemy_async_integration.py</code>.</li> <li>Pure Python unit tests (no DB) go in <code>tests/test_sql_conversion.py</code>.</li> </ul> <p>Tests that depend on bleeding-edge PGMQ features must gracefully skip when <code>UndefinedFunction</code> or <code>RaiseException</code> is raised, because CI images may lag behind the extension.</p>"},{"location":"development/#building-documentation","title":"Building Documentation","text":"<p>This documentation is designed for MkDocs with the Material theme.</p>"},{"location":"development/#install-mkdocs","title":"Install MkDocs","text":"<pre><code>pip install mkdocs mkdocs-material mike\n</code></pre>"},{"location":"development/#serve-locally","title":"Serve Locally","text":"<pre><code>mkdocs serve\n</code></pre>"},{"location":"development/#build","title":"Build","text":"<pre><code>mkdocs build\n</code></pre>"},{"location":"development/#versioned-deployment-mike","title":"Versioned Deployment (Mike)","text":"<p>This project uses <code>mike</code> to deploy versioned documentation.</p> <pre><code># Deploy the current version\nmike deploy 1.1.0 latest\n\n# Set the default version\nmike set-default latest\n</code></pre> <p><code>mike</code> stores versions as separate commits on the <code>gh-pages</code> branch and generates a version switcher in the UI.</p>"},{"location":"development/#contributing-guidelines","title":"Contributing Guidelines","text":"<ul> <li>Follow the existing code style (<code>ruff</code> enforces this).</li> <li>Add docstrings to all public methods.</li> <li>Use <code>log_with_context</code> instead of bare <code>print()</code> in library code.</li> <li>Keep SQL centralized in <code>src/pgmq/_sql.py</code>.</li> <li>Update <code>pyproject.toml</code> version when releasing.</li> <li>Add tests for new features.</li> </ul>"},{"location":"getting_started/","title":"Getting Started","text":""},{"location":"getting_started/#prerequisites","title":"Prerequisites","text":"<ul> <li>Python 3.9 or newer.</li> <li>A running PostgreSQL instance with the PGMQ extension installed.</li> </ul>"},{"location":"getting_started/#installation","title":"Installation","text":"<p>Install the base package from PyPI:</p> <pre><code>pip install pgmq\n</code></pre>"},{"location":"getting_started/#optional-extras","title":"Optional Extras","text":"Extra Command Backend Async <code>pip install pgmq[async]</code> <code>asyncpg</code> SQLAlchemy (sync) <code>pip install pgmq[sqlalchemy]</code> SQLAlchemy + <code>psycopg</code> SQLAlchemy (async) <code>pip install pgmq[sqlalchemy-async]</code> SQLAlchemy + <code>asyncpg</code> <p>You can also install everything at once:</p> <pre><code>pip install pgmq[async,sqlalchemy,sqlalchemy-async]\n</code></pre>"},{"location":"getting_started/#start-a-local-postgres-with-pgmq","title":"Start a Local Postgres with PGMQ","text":""},{"location":"getting_started/#docker-recommended","title":"Docker (recommended)","text":"<p>The fastest way to get started is with the pre-built Docker image, where PGMQ comes pre-installed as an extension in Postgres:</p> <pre><code>docker run -d --name pgmq-postgres \\\n  -e POSTGRES_PASSWORD=postgres \\\n  -p 5432:5432 \\\n  ghcr.io/pgmq/pg18-pgmq:latest\n</code></pre> <p>Then connect and enable PGMQ:</p> <pre><code>psql postgres://postgres:postgres@localhost:5432/postgres -c \"CREATE EXTENSION pgmq;\"\n</code></pre>"},{"location":"getting_started/#sql-only","title":"SQL Only","text":"<p>You can also use <code>psql</code> to install PGMQ's objects directly into the <code>pgmq</code> schema in Postgres.  Use this method in environments that do not natively support the PGMQ extension or where extension management is handled manually.</p> <pre><code>git clone https://github.com/pgmq/pgmq.git\ncd pgmq\npsql -f pgmq-extension/sql/pgmq.sql postgres://postgres:postgres@localhost:5432/postgres\n</code></pre> <p>In these environments, automatic extension initialization can be disabled by setting <code>init_extension=False</code> when creating the client instance.</p> <p>Example:</p> <pre><code>from pgmq import PGMQueue\n\nclient = PGMQueue(\n    host=host,\n    port=port,\n    username=username,\n    password=password,\n    database=database,\n    init_extension=False\n)\n</code></pre> <p>When <code>init_extension=False</code> is set, the client will not attempt to run:</p> <pre><code>CREATE EXTENSION IF NOT EXISTS pgmq CASCADE;\n</code></pre> <p>This option is supported by both sync and async clients.</p>"},{"location":"getting_started/#your-first-queue","title":"Your First Queue","text":"<p>Create a file named <code>quickstart.py</code>:</p> <pre><code>from pgmq import PGMQueue\n\n# Connect using environment variables or defaults\nqueue = PGMQueue()\n\n# Create a queue\nqueue.create_queue(\"my_queue\")\n\n# Send a message\nmsg_id = queue.send(\"my_queue\", {\"task\": \"send_email\", \"to\": \"user@example.com\"})\nprint(f\"Sent message {msg_id}\")\n\n# Read a message (invisible for 30 seconds)\nmsg = queue.read(\"my_queue\", vt=30)\nprint(f\"Received: {msg.message}\")\n\n# Archive it when done\nqueue.archive(\"my_queue\", msg.msg_id)\n</code></pre> <p>Run it:</p> <pre><code>python quickstart.py\n</code></pre>"},{"location":"getting_started/#environment-variables","title":"Environment Variables","text":"<p>If you prefer not to hard-code credentials, set these variables before running your script:</p> <pre><code>export PG_HOST=127.0.0.1\nexport PG_PORT=5432\nexport PG_USERNAME=postgres\nexport PG_PASSWORD=postgres\nexport PG_DATABASE=postgres\n# Or use a full connection string:\nexport DATABASE_URL=postgresql://postgres:postgres@localhost:5432/postgres\n</code></pre>"},{"location":"getting_started/#next-steps","title":"Next Steps","text":"<ul> <li>Learn how to configure the client programmatically.</li> <li>Explore the four available client backends.</li> <li>Understand transactions for atomic multi-operation workflows.</li> </ul>"},{"location":"logging/","title":"Logging","text":"<p><code>pgmq</code> uses a dual-backend logging system that works out of the box with Python's standard <code>logging</code> module, while optionally supporting <code>loguru</code> when installed.</p>"},{"location":"logging/#default-behavior","title":"Default Behavior","text":"<p>By default, the library does not attach any handlers or change global logging levels. It simply acquires a logger via <code>logging.getLogger(name)</code> and emits <code>DEBUG</code> level messages during normal operation. If you do not configure logging, these messages are silently ignored.</p>"},{"location":"logging/#enabling-verbose-logging","title":"Enabling Verbose Logging","text":"<p>Pass <code>verbose=True</code> to see <code>DEBUG</code> output on stderr and in a log file:</p> <pre><code>queue = PGMQueue(verbose=True, log_filename=\"pgmq.log\")\n</code></pre>"},{"location":"logging/#structured-logging","title":"Structured Logging","text":"<p>Output JSON-formatted logs for ingestion by log aggregators:</p> <pre><code>queue = PGMQueue(\n    verbose=True,\n    structured_logging=True,\n    log_filename=\"pgmq.jsonl\",\n)\n</code></pre>"},{"location":"logging/#log-rotation-loguru","title":"Log Rotation (loguru)","text":"<p>If <code>loguru</code> is installed, you can enable rotation and retention:</p> <pre><code>queue = PGMQueue(\n    verbose=True,\n    log_rotation=True,\n    log_rotation_size=\"10 MB\",\n    log_retention=\"1 week\",\n)\n</code></pre>"},{"location":"logging/#global-logging-configuration","title":"Global Logging Configuration","text":"<p>You can apply settings globally via <code>LoggingManager</code>:</p> <pre><code>from pgmq.logger import LoggingManager\n\nLoggingManager.configure_global_logging(\n    log_level=\"INFO\",\n    structured=True,\n    use_loguru=True,  # requires loguru to be installed\n)\n</code></pre>"},{"location":"logging/#performance-logging","title":"Performance Logging","text":"<p>The <code>log_performance</code> decorator records execution time and success/failure status:</p> <pre><code>from pgmq.logger import log_performance\n\nlogger = queue.logger\n\n@log_performance(logger)\ndef process_message(msg):\n    ...\n</code></pre> <p>It works with both sync and async functions.</p>"},{"location":"logging/#contextual-logging","title":"Contextual Logging","text":"<p>Add structured context to any log entry:</p> <pre><code>from pgmq.logger import log_with_context\n\nlog_with_context(\n    queue.logger,\n    \"INFO\",\n    \"Processing batch\",\n    batch_size=100,\n    queue=\"my_queue\",\n)\n</code></pre> <p>Both stdlib <code>logging</code> and <code>loguru</code> backends are handled transparently.</p>"},{"location":"logging/#logger-types","title":"Logger Types","text":"Symbol Description <code>LoggingManager</code> Centralized logger factory and configuration. <code>PGMQLogger</code> Backward-compatible alias for <code>LoggingManager</code>. <code>create_logger()</code> Factory function for quick logger creation."},{"location":"logging/#test-mode","title":"Test Mode","text":"<p><code>LoggingManager._test_mode = True</code> disables <code>enqueue</code> in loguru handlers, ensuring synchronous logging for test assertions. This is used internally by the test suite.</p>"},{"location":"messages/","title":"Messages and Dataclasses","text":"<p>The <code>pgmq</code> library maps PGMQ PostgreSQL composite types to Python <code>@dataclass</code> objects for type safety and IDE autocomplete support.</p>"},{"location":"messages/#message","title":"Message","text":"<p>Represents a single message record.</p> <pre><code>from pgmq import Message\n\nmsg: Message = queue.read(\"my_queue\", vt=30)\n</code></pre> Attribute Type Description <code>msg_id</code> <code>int</code> Unique message identifier. <code>read_ct</code> <code>int</code> Number of times the message has been read. <code>enqueued_at</code> <code>datetime</code> Timestamp when the message was sent. <code>last_read_at</code> <code>datetime \\| None</code> Timestamp of the most recent read. <code>vt</code> <code>datetime</code> Visibility timeout expiration timestamp. <code>message</code> <code>dict</code> The JSON payload. <code>headers</code> <code>dict \\| None</code> Optional metadata headers."},{"location":"messages/#queuerecord","title":"QueueRecord","text":"<p>Metadata about a queue.</p> <pre><code>queues = queue.list_queues()\nfor q in queues:\n    print(q.queue_name)\n</code></pre> Attribute Type Description <code>queue_name</code> <code>str</code> Queue name. <code>is_partitioned</code> <code>bool</code> Partitioned flag. <code>is_unlogged</code> <code>bool</code> Unlogged flag. <code>created_at</code> <code>datetime</code> Creation timestamp. <p>Casting to <code>str</code> returns the queue name for convenience:</p> <pre><code>str(q) == q.queue_name\n</code></pre>"},{"location":"messages/#queuemetrics","title":"QueueMetrics","text":"<p>Statistics returned by <code>metrics()</code> and <code>metrics_all()</code>.</p> <pre><code>metrics = queue.metrics(\"my_queue\")\nprint(metrics.queue_length)\n</code></pre> Attribute Type Description <code>queue_name</code> <code>str</code> Queue name. <code>queue_length</code> <code>int</code> Total messages in the queue. <code>newest_msg_age_sec</code> <code>int \\| None</code> Age of the newest message in seconds. <code>oldest_msg_age_sec</code> <code>int \\| None</code> Age of the oldest message in seconds. <code>total_messages</code> <code>int</code> Total messages ever sent. <code>scrape_time</code> <code>datetime</code> When the metrics were collected. <code>queue_visible_length</code> <code>int</code> Messages currently visible (not invisible due to VT)."},{"location":"messages/#topicbinding","title":"TopicBinding","text":"<p>Represents a topic routing binding.</p> <pre><code>bindings = queue.list_topic_bindings()\nfor b in bindings:\n    print(b.pattern, b.queue_name, b.compiled_regex)\n</code></pre> Attribute Type Description <code>pattern</code> <code>str</code> The binding pattern (e.g., <code>orders.*</code>). <code>queue_name</code> <code>str</code> Target queue. <code>bound_at</code> <code>datetime</code> When the binding was created. <code>compiled_regex</code> <code>str</code> The internal regex representation."},{"location":"messages/#routingresult","title":"RoutingResult","text":"<p>Result from <code>test_routing()</code>.</p> Attribute Type Description <code>pattern</code> <code>str</code> Matched pattern. <code>queue_name</code> <code>str</code> Target queue. <code>compiled_regex</code> <code>str</code> Internal regex."},{"location":"messages/#batchtopicresult","title":"BatchTopicResult","text":"<p>Result from <code>send_batch_topic()</code>.</p> Attribute Type Description <code>queue_name</code> <code>str</code> Queue that received the message. <code>msg_id</code> <code>int</code> Assigned message ID."},{"location":"messages/#notificationthrottle","title":"NotificationThrottle","text":"<p>Configuration for NOTIFY throttling.</p> <pre><code>throttles = queue.list_notify_throttles()\nfor t in throttles:\n    print(t.queue_name, t.throttle_interval_ms)\n</code></pre> Attribute Type Description <code>queue_name</code> <code>str</code> Queue name. <code>throttle_interval_ms</code> <code>int</code> Minimum milliseconds between notifications. <code>last_notified_at</code> <code>datetime</code> Last notification timestamp."},{"location":"metrics/","title":"Metrics","text":"<p>PGMQ exposes queue-level metrics that are useful for monitoring, alerting, and capacity planning.</p>"},{"location":"metrics/#queue-metrics","title":"Queue Metrics","text":"<p>Retrieve statistics for a specific queue:</p> <pre><code>metrics = queue.metrics(\"my_queue\")\nprint(f\"Length: {metrics.queue_length}\")\nprint(f\"Visible: {metrics.queue_visible_length}\")\nprint(f\"Oldest: {metrics.oldest_msg_age_sec}s\")\n</code></pre>"},{"location":"metrics/#all-queue-metrics","title":"All Queue Metrics","text":"<p>Retrieve metrics for every queue in the database:</p> <pre><code>all_metrics = queue.metrics_all()\nfor m in all_metrics:\n    print(m.queue_name, m.queue_length, m.total_messages)\n</code></pre>"},{"location":"metrics/#metrics-reference","title":"Metrics Reference","text":"<p>The <code>QueueMetrics</code> dataclass contains the following fields:</p> Field Type Description <code>queue_name</code> <code>str</code> Queue name. <code>queue_length</code> <code>int</code> Total messages in the queue (including invisible). <code>newest_msg_age_sec</code> <code>int \\| None</code> Age of the newest message in seconds. <code>oldest_msg_age_sec</code> <code>int \\| None</code> Age of the oldest message in seconds. <code>total_messages</code> <code>int</code> Total messages ever sent to the queue. <code>scrape_time</code> <code>datetime</code> Timestamp when metrics were collected. <code>queue_visible_length</code> <code>int</code> Messages currently visible (not hidden by VT). <p>Schema compatibility: The client handles both old (6-column) and new (7-column) <code>pgmq.metrics</code> result schemas automatically.</p>"},{"location":"notifications/","title":"Notifications","text":"<p>PGMQ can emit PostgreSQL <code>NOTIFY</code> events when new messages are inserted into a queue. This enables real-time, push-style consumers without constant polling.</p>"},{"location":"notifications/#enabling-notifications","title":"Enabling Notifications","text":"<pre><code>queue.enable_notify(\"my_queue\", throttle_interval_ms=250)\n</code></pre> Parameter Description <code>throttle_interval_ms</code> Minimum time between notifications for this queue. Prevents notification storms."},{"location":"notifications/#disabling-notifications","title":"Disabling Notifications","text":"<pre><code>queue.disable_notify(\"my_queue\")\n</code></pre>"},{"location":"notifications/#updating-throttle-settings","title":"Updating Throttle Settings","text":"<pre><code>queue.update_notify(\"my_queue\", throttle_interval_ms=500)\n</code></pre>"},{"location":"notifications/#listing-notification-configurations","title":"Listing Notification Configurations","text":"<pre><code>throttles = queue.list_notify_throttles()\nfor t in throttles:\n    print(t.queue_name, t.throttle_interval_ms, t.last_notified_at)\n</code></pre>"},{"location":"notifications/#notification-listeners","title":"Notification Listeners","text":"<p>The library provides two listener implementations that wrap PostgreSQL <code>LISTEN</code>:</p>"},{"location":"notifications/#sync-listener","title":"Sync Listener","text":"<pre><code>from pgmq.notify_listener import SyncNotificationListener\n\nlistener = SyncNotificationListener(queue)\n\ndef on_message(payload):\n    print(\"Received:\", payload)\n\n# Blocks until timeout or stop signal\nlistener.listen(\"my_queue\", callback=on_message, timeout=30.0)\n</code></pre>"},{"location":"notifications/#async-listener","title":"Async Listener","text":"<pre><code>from pgmq.notify_listener import AsyncNotificationListener\n\nlistener = AsyncNotificationListener(queue)\n\nasync def on_message(payload):\n    print(\"Received:\", payload)\n\n# Run in an asyncio task\nawait listener.listen(\"my_queue\", callback=on_message)\n</code></pre>"},{"location":"notifications/#stopping-a-listener","title":"Stopping a Listener","text":"<p>Both listeners support a <code>stop()</code> method:</p> <pre><code>listener.stop()\n</code></pre> <p>This sets an internal event and closes the underlying connection, causing the listening loop to exit cleanly.</p>"},{"location":"notifications/#payload-format","title":"Payload Format","text":"<p>The notification payload is a JSON object. If the payload is empty or cannot be parsed, the callback still fires with a minimal dict:</p> <pre><code>{\"event\": \"insert\", \"payload_empty\": True}\n# or\n{\"event\": \"insert\", \"raw_payload\": \"...\", \"parse_error\": \"...\"}\n</code></pre>"},{"location":"notifications/#channel-naming","title":"Channel Naming","text":"<p>The listener automatically listens on the channel:</p> <pre><code>pgmq.q_&lt;queue_name&gt;.INSERT\n</code></pre> <p>Channel names are safely quoted to prevent identifier injection.</p>"},{"location":"queue_management/","title":"Queue Management","text":""},{"location":"queue_management/#creating-a-queue","title":"Creating a Queue","text":"<pre><code>queue.create_queue(\"my_queue\")\n</code></pre>"},{"location":"queue_management/#unlogged-queues","title":"Unlogged Queues","text":"<p>Unlogged queues are faster but do not survive a database crash. Use them for transient, high-throughput data.</p> <pre><code>queue.create_queue(\"temp_queue\", unlogged=True)\n</code></pre>"},{"location":"queue_management/#partitioned-queues","title":"Partitioned Queues","text":"<p>Partitioned queues split data across multiple tables, improving performance for very large queues.</p> <pre><code>queue.create_partitioned_queue(\n    \"big_queue\",\n    partition_interval=10000,\n    retention_interval=100000,\n)\n</code></pre> Parameter Description <code>partition_interval</code> Number of messages per partition. <code>retention_interval</code> Number of messages to retain in the active partition set."},{"location":"queue_management/#dropping-a-queue","title":"Dropping a Queue","text":"<pre><code>dropped: bool = queue.drop_queue(\"my_queue\")\n</code></pre> <p>Dropping a queue removes the queue table and its archive table. Returns <code>True</code> on success.</p>"},{"location":"queue_management/#listing-queues","title":"Listing Queues","text":"<pre><code>queues = queue.list_queues()\nfor q in queues:\n    print(q.queue_name, q.is_partitioned, q.created_at)\n</code></pre> <p><code>list_queues()</code> returns a list of <code>QueueRecord</code> objects:</p> Attribute Type Description <code>queue_name</code> <code>str</code> Name of the queue. <code>is_partitioned</code> <code>bool</code> Whether the queue is partitioned. <code>is_unlogged</code> <code>bool</code> Whether the queue is unlogged. <code>created_at</code> <code>datetime</code> Creation timestamp. <p>Backward compatibility note: Older versions returned <code>List[str]</code>. The current version returns <code>List[QueueRecord]</code> and emits a <code>UserWarning</code> on the sync client. Use <code>.queue_name</code> to get the string name.</p>"},{"location":"queue_management/#purging-a-queue","title":"Purging a Queue","text":"<p>Remove all messages from a queue without deleting the queue itself:</p> <pre><code>purged_count: int = queue.purge(\"my_queue\")\n</code></pre>"},{"location":"queue_management/#validating-a-queue-name","title":"Validating a Queue Name","text":"<pre><code>queue.validate_queue_name(\"my_queue\")  # Raises if invalid\n</code></pre> <p>This delegates validation to <code>pgmq.validate_queue_name()</code> in the database, ensuring your queue name complies with PGMQ naming rules.</p>"},{"location":"reading_messages/","title":"Reading Messages","text":""},{"location":"reading_messages/#basic-read","title":"Basic Read","text":"<p>Read one or more messages from a queue. The messages become invisible to other readers for the duration of the visibility timeout (<code>vt</code>).</p> <pre><code># Read a single message\nmsg = queue.read(\"my_queue\", vt=30)\n\n# Read multiple messages\nmsgs = queue.read(\"my_queue\", vt=30, qty=5)\n</code></pre> Parameter Description <code>vt</code> Visibility timeout in seconds. Defaults to the client's <code>config.vt</code> (30). <code>qty</code> Number of messages to read. Defaults to <code>1</code>."},{"location":"reading_messages/#return-type","title":"Return Type","text":"<ul> <li>If <code>qty == 1</code>, returns <code>Message | None</code>.</li> <li>If <code>qty &gt; 1</code>, returns <code>list[Message]</code>.</li> </ul>"},{"location":"reading_messages/#read-batch-backward-compatibility","title":"Read Batch (Backward Compatibility)","text":"<p><code>read_batch</code> is an alias for <code>read</code> with <code>batch_size</code> mapped to <code>qty</code>:</p> <pre><code>msgs = queue.read_batch(\"my_queue\", vt=30, batch_size=5)\n</code></pre> <p>Always returns a <code>list[Message]</code> (empty if nothing found).</p>"},{"location":"reading_messages/#read-with-polling","title":"Read with Polling","text":"<p>Repeatedly poll the queue until messages are found or the timeout expires.</p> <pre><code>msgs = queue.read_with_poll(\n    \"my_queue\",\n    vt=30,\n    qty=5,\n    max_poll_seconds=5,\n    poll_interval_ms=100,\n)\n</code></pre> Parameter Description <code>max_poll_seconds</code> Maximum total time to keep polling. <code>poll_interval_ms</code> Wait time between successive polls."},{"location":"reading_messages/#conditional-reads","title":"Conditional Reads","text":"<p>Read messages matching a JSONB condition. This is useful when messages contain a <code>headers</code> field or specific payload shapes.</p> <pre><code>msgs = queue.read(\n    \"my_queue\",\n    vt=30,\n    qty=10,\n    conditional={\"status\": \"pending\"},\n)\n</code></pre> <p>Conditional reads also work with <code>read_with_poll</code>.</p>"},{"location":"reading_messages/#fifo-reads","title":"FIFO Reads","text":"<p>PGMQ supports FIFO (First-In-First-Out) semantics for ordered processing.</p>"},{"location":"reading_messages/#grouped-read","title":"Grouped Read","text":"<p>SQS-style batch filling that groups related messages:</p> <pre><code>msgs = queue.read_grouped(\"my_queue\", vt=30, qty=10)\n</code></pre>"},{"location":"reading_messages/#grouped-read-with-poll","title":"Grouped Read with Poll","text":"<pre><code>msgs = queue.read_grouped_with_poll(\n    \"my_queue\", vt=30, qty=10, max_poll_seconds=5, poll_interval_ms=100\n)\n</code></pre>"},{"location":"reading_messages/#round-robin-read","title":"Round-Robin Read","text":"<p>Interleave messages across partitions for balanced consumption:</p> <pre><code>msgs = queue.read_grouped_rr(\"my_queue\", vt=30, qty=10)\n</code></pre>"},{"location":"reading_messages/#round-robin-read-with-poll","title":"Round-Robin Read with Poll","text":"<pre><code>msgs = queue.read_grouped_rr_with_poll(\n    \"my_queue\", vt=30, qty=10, max_poll_seconds=5, poll_interval_ms=100\n)\n</code></pre> <p>FIFO operations require the queue to have a FIFO index. See Utilities for <code>create_fifo_index</code>.</p>"},{"location":"reading_messages/#handling-the-result","title":"Handling the Result","text":"<pre><code>msg = queue.read(\"my_queue\", vt=30)\nif msg is None:\n    print(\"No messages available\")\nelse:\n    print(f\"ID: {msg.msg_id}, Body: {msg.message}\")\n    # Process the message, then archive or delete it\n    queue.archive(\"my_queue\", msg.msg_id)\n</code></pre>"},{"location":"sending_messages/","title":"Sending Messages","text":""},{"location":"sending_messages/#send-a-single-message","title":"Send a Single Message","text":"<pre><code>msg_id: int = queue.send(\"my_queue\", {\"task\": \"process_payment\"})\n</code></pre>"},{"location":"sending_messages/#send-with-headers","title":"Send with Headers","text":"<p>Headers are optional metadata dictionaries attached to the message:</p> <pre><code>msg_id = queue.send(\n    \"my_queue\",\n    {\"task\": \"process_payment\"},\n    headers={\"priority\": \"high\", \"source\": \"webhook\"},\n)\n</code></pre>"},{"location":"sending_messages/#send-with-delay","title":"Send with Delay","text":"<p>Delay message delivery by an integer number of seconds or a specific <code>datetime</code>:</p> <pre><code># Delay by 60 seconds\nmsg_id = queue.send(\"my_queue\", {\"task\": \"reminder\"}, delay=60)\n\n# Delay until a specific time\nfrom datetime import datetime, timezone\nfuture = datetime.now(timezone.utc).replace(hour=23, minute=0)\nmsg_id = queue.send(\"my_queue\", {\"task\": \"nightly_report\"}, delay=future)\n</code></pre>"},{"location":"sending_messages/#backward-compatible-alias","title":"Backward Compatible Alias","text":"<p>The <code>tz</code> parameter is an alias for <code>delay</code> and exists for backward compatibility:</p> <pre><code>msg_id = queue.send(\"my_queue\", {\"task\": \"reminder\"}, tz=60)\n</code></pre>"},{"location":"sending_messages/#send-a-batch","title":"Send a Batch","text":"<p>Send multiple messages in one transaction for higher throughput:</p> <pre><code>messages = [\n    {\"task\": \"email_user_1\"},\n    {\"task\": \"email_user_2\"},\n    {\"task\": \"email_user_3\"},\n]\n\nmsg_ids: list[int] = queue.send_batch(\"my_queue\", messages)\n</code></pre>"},{"location":"sending_messages/#batch-with-headers","title":"Batch with Headers","text":"<pre><code>headers = [\n    {\"priority\": \"high\"},\n    {\"priority\": \"low\"},\n    {\"priority\": \"high\"},\n]\nmsg_ids = queue.send_batch(\"my_queue\", messages, headers=headers)\n</code></pre> <p>The <code>headers</code> list must be the same length as <code>messages</code>.</p>"},{"location":"sending_messages/#batch-with-delay","title":"Batch with Delay","text":"<pre><code>msg_ids = queue.send_batch(\"my_queue\", messages, delay=300)\n</code></pre>"},{"location":"sending_messages/#return-value","title":"Return Value","text":"<ul> <li><code>send</code> returns a single <code>int</code> (message ID).</li> <li><code>send_batch</code> returns <code>list[int]</code>.</li> </ul> <p>On failure or unsupported configurations, <code>-1</code> or an empty list may be returned.</p>"},{"location":"topic_routing/","title":"Topic-Based Routing","text":"<p>Topic routing allows you to send a message once and have it delivered to multiple queues based on pattern matching. This is useful for pub/sub and event-driven architectures.</p>"},{"location":"topic_routing/#concepts","title":"Concepts","text":"<ul> <li>Routing Key: A dot-separated string describing the event (e.g., <code>orders.created</code>).</li> <li>Pattern: A binding rule using <code>*</code> (single word) or <code>#</code> (zero or more words) wildcards.</li> <li>Binding: Associating a pattern with a queue.</li> </ul>"},{"location":"topic_routing/#bind-a-pattern","title":"Bind a Pattern","text":"<pre><code>queue.bind_topic(\"orders.*\", \"orders_queue\")\nqueue.bind_topic(\"orders.#\", \"audit_queue\")\n</code></pre> Pattern Matches <code>orders.*</code> <code>orders.created</code>, <code>orders.updated</code> (one word) <code>orders.#</code> <code>orders.created</code>, <code>orders.created.us</code> (any depth) <code>*.error</code> <code>payment.error</code>, <code>shipping.error</code>"},{"location":"topic_routing/#unbind-a-pattern","title":"Unbind a Pattern","text":"<pre><code>queue.unbind_topic(\"orders.*\", \"orders_queue\")\n</code></pre>"},{"location":"topic_routing/#send-to-a-topic","title":"Send to a Topic","text":"<pre><code>queue.send_topic(\"orders.created\", {\"order_id\": 123, \"total\": 99.99})\n</code></pre> <p>This delivers the message to all queues whose bound patterns match the routing key.</p>"},{"location":"topic_routing/#send-with-headers","title":"Send with Headers","text":"<pre><code>queue.send_topic(\n    \"orders.created\",\n    {\"order_id\": 123},\n    headers={\"source\": \"web\"},\n)\n</code></pre>"},{"location":"topic_routing/#send-with-delay","title":"Send with Delay","text":"<pre><code>queue.send_topic(\"orders.created\", {\"order_id\": 123}, delay=60)\n</code></pre>"},{"location":"topic_routing/#send-batch-to-a-topic","title":"Send Batch to a Topic","text":"<pre><code>messages = [\n    {\"order_id\": 1},\n    {\"order_id\": 2},\n    {\"order_id\": 3},\n]\nresults = queue.send_batch_topic(\"orders.created\", messages)\nfor r in results:\n    print(r.queue_name, r.msg_id)\n</code></pre>"},{"location":"topic_routing/#list-bindings","title":"List Bindings","text":"<pre><code># All bindings\nbindings = queue.list_topic_bindings()\n\n# Filtered by queue\nbindings = queue.list_topic_bindings(queue_name=\"orders_queue\")\n</code></pre> <p>Returns <code>list[TopicBinding]</code>.</p>"},{"location":"topic_routing/#test-routing","title":"Test Routing","text":"<p>Preview which queues would receive a message without actually sending it:</p> <pre><code>results = queue.test_routing(\"orders.created\")\nfor r in results:\n    print(r.queue_name, r.pattern)\n</code></pre>"},{"location":"topic_routing/#validation","title":"Validation","text":"<p>Validate routing keys and patterns before using them:</p> <pre><code>queue.validate_routing_key(\"orders.created\")  # True or False\nqueue.validate_topic_pattern(\"orders.*\")      # True or False\n</code></pre> <p>These return booleans and will not raise exceptions on invalid input.</p>"},{"location":"transactions/","title":"Transactions","text":"<p>All write operations in <code>pgmq</code> are transactional by default thanks to the built-in decorators. You can also compose multiple operations into a single transaction by passing a connection object manually.</p>"},{"location":"transactions/#transaction-decorators","title":"Transaction Decorators","text":"<p>Four decorators are provided, one for each client backend:</p> <pre><code>from pgmq.decorators import (\n    transaction,              # psycopg sync\n    async_transaction,        # asyncpg\n    sqlalchemy_transaction,   # SQLAlchemy sync\n    sqlalchemy_async_transaction,  # SQLAlchemy async\n)\n</code></pre>"},{"location":"transactions/#how-decorators-work","title":"How Decorators Work","text":"<ol> <li>Check if <code>conn</code> is already provided in <code>kwargs</code>.</li> <li>If not, acquire a connection (and start a transaction) from the pool or engine.</li> <li>Inject <code>conn</code> into the function.</li> <li>Commit on success, rollback on exception.</li> </ol>"},{"location":"transactions/#example-automatic-transaction","title":"Example: Automatic Transaction","text":"<pre><code>from pgmq import PGMQueue\nfrom pgmq.decorators import transaction\n\nqueue = PGMQueue()\n\n@transaction\ndef send_and_read(queue, conn=None):\n    queue.create_queue(\"tx_queue\", conn=conn)\n    msg_id = queue.send(\"tx_queue\", {\"hello\": \"world\"}, conn=conn)\n    msg = queue.read(\"tx_queue\", vt=30, conn=conn)\n    return msg\n\nmsg = send_and_read(queue)\n</code></pre>"},{"location":"transactions/#example-async-transaction","title":"Example: Async Transaction","text":"<pre><code>from pgmq import AsyncPGMQueue\nfrom pgmq.decorators import async_transaction\n\nqueue = AsyncPGMQueue()\nawait queue.init()\n\n@async_transaction\nasync def send_and_read(queue, conn=None):\n    await queue.create_queue(\"tx_queue\", conn=conn)\n    msg_id = await queue.send(\"tx_queue\", {\"hello\": \"world\"}, conn=conn)\n    msg = await queue.read(\"tx_queue\", vt=30, conn=conn)\n    return msg\n\nmsg = await send_and_read(queue)\n</code></pre>"},{"location":"transactions/#manual-connection-passing","title":"Manual Connection Passing","text":"<p>You can bypass decorators and manage transactions yourself by passing <code>conn</code> to each operation:</p> <pre><code>with queue.pool.connection() as conn:\n    with conn.transaction():\n        queue.send(\"my_queue\", {\"a\": 1}, conn=conn)\n        queue.send(\"my_queue\", {\"b\": 2}, conn=conn)\n</code></pre> <p>For SQLAlchemy:</p> <pre><code>with queue.engine.begin() as conn:\n    queue.send(\"my_queue\", {\"a\": 1}, conn=conn)\n    queue.send(\"my_queue\", {\"b\": 2}, conn=conn)\n</code></pre>"},{"location":"transactions/#nested-transactions","title":"Nested Transactions","text":"<p>Decorators detect when <code>conn</code> is already present and will not start a new transaction, allowing safe nesting:</p> <pre><code>@transaction\ndef outer(queue, conn=None):\n    inner(queue, conn=conn)  # reuses the same connection\n\n@transaction\ndef inner(queue, conn=None):\n    queue.send(\"my_queue\", {\"nested\": True}, conn=conn)\n</code></pre>"},{"location":"transactions/#read-only-operations","title":"Read-Only Operations","text":"<p>Methods that do not modify data (e.g., <code>list_queues</code>, <code>metrics</code>, <code>test_routing</code>) typically do not use the transaction decorator. You can still pass <code>conn</code> if you want them to run inside your managed transaction scope.</p>"},{"location":"utilities/","title":"Utilities","text":""},{"location":"utilities/#validation-helpers","title":"Validation Helpers","text":""},{"location":"utilities/#validate-routing-key","title":"Validate Routing Key","text":"<pre><code>is_valid = queue.validate_routing_key(\"orders.created\")\n</code></pre> <p>Returns <code>True</code> if the routing key conforms to PGMQ rules, <code>False</code> otherwise.</p>"},{"location":"utilities/#validate-topic-pattern","title":"Validate Topic Pattern","text":"<pre><code>is_valid = queue.validate_topic_pattern(\"orders.*\")\n</code></pre> <p>Returns <code>True</code> if the pattern is valid for topic bindings.</p>"},{"location":"utilities/#fifo-indexes","title":"FIFO Indexes","text":"<p>FIFO indexes improve performance for ordered read operations (<code>read_grouped</code>, <code>read_grouped_rr</code>).</p>"},{"location":"utilities/#create-index-for-a-single-queue","title":"Create Index for a Single Queue","text":"<pre><code>queue.create_fifo_index(\"my_queue\")\n</code></pre>"},{"location":"utilities/#create-indexes-for-all-queues","title":"Create Indexes for All Queues","text":"<pre><code>queue.create_fifo_indexes_all()\n</code></pre>"},{"location":"utilities/#archive-conversion","title":"Archive Conversion","text":""},{"location":"utilities/#convert-archive-to-partitioned","title":"Convert Archive to Partitioned","text":"<p>For queues with very large archive tables, you can convert the archive to a partitioned table:</p> <pre><code>queue.convert_archive_partitioned(\n    \"my_queue\",\n    partition_interval=10000,\n    retention_interval=100000,\n    leading_partition=10,\n)\n</code></pre>"},{"location":"utilities/#detach-archive","title":"Detach Archive","text":"<p>Detach the archive table from PGMQ management. This is deprecated in PGMQ v2.0 but retained for backward compatibility.</p> <pre><code>queue.detach_archive(\"my_queue\")\n</code></pre>"},{"location":"utilities/#sql-conversion-helpers","title":"SQL Conversion Helpers","text":"<p>The internal <code>_sql.py</code> module provides utilities used by the async and SQLAlchemy backends. These are generally not needed by end users, but are documented here for contributors:</p> <ul> <li><code>_convert_psycopg_to_asyncpg(sql)</code>: Converts <code>%s</code> placeholders to <code>$1</code>, <code>$2</code>, etc.</li> <li><code>convert_sql_params(sql, params)</code>: Converts psycopg-style SQL + tuple params to SQLAlchemy <code>:param_N</code> + dict form, with special JSONB handling.</li> <li><code>ASYNC_SQL_MAP</code>: Pre-computed dictionary of asyncpg-compatible SQL templates.</li> </ul>"},{"location":"utilities/#client-lifecycle","title":"Client Lifecycle","text":""},{"location":"utilities/#async-clients","title":"Async Clients","text":"<p>Always close async clients to release pool resources:</p> <pre><code>await queue.close()\n</code></pre>"},{"location":"utilities/#sqlalchemy-sync-client","title":"SQLAlchemy Sync Client","text":"<p>Dispose of the engine and close all connections:</p> <pre><code>queue.dispose()\n</code></pre> <p>If you passed an external engine, calling <code>dispose()</code> will also dispose it. Skip this call if the engine lifecycle is managed elsewhere.</p>"},{"location":"visibility_timeout/","title":"Visibility Timeout","text":"<p>The visibility timeout (VT) determines how long a message remains invisible to other consumers after being read. If the consumer does not delete or archive the message before the VT expires, the message becomes visible again.</p>"},{"location":"visibility_timeout/#set-vt-for-a-single-message","title":"Set VT for a Single Message","text":"<pre><code>updated = queue.set_vt(\"my_queue\", msg_id=42, vt=120)\n</code></pre> <p>Returns the updated <code>Message</code> object.</p>"},{"location":"visibility_timeout/#set-vt-for-multiple-messages","title":"Set VT for Multiple Messages","text":"<pre><code>updated = queue.set_vt(\"my_queue\", msg_id=[42, 43, 44], vt=120)\n</code></pre> <p>Returns a <code>list[Message]</code>.</p>"},{"location":"visibility_timeout/#set-vt-to-a-specific-timestamp","title":"Set VT to a Specific Timestamp","text":"<p>Instead of an integer number of seconds, you can pass a <code>datetime</code> to set an absolute expiration:</p> <pre><code>from datetime import datetime, timezone, timedelta\n\nfuture = datetime.now(timezone.utc) + timedelta(minutes=10)\nupdated = queue.set_vt(\"my_queue\", msg_id=42, vt=future)\n</code></pre>"},{"location":"visibility_timeout/#behavior-notes","title":"Behavior Notes","text":"<ul> <li><code>set_vt</code> is transactional. If the operation fails, no changes are applied.</li> <li>The returned <code>Message.vt</code> reflects the new expiration time.</li> <li>Use this to implement \"heartbeat\" or \"lease extension\" patterns in long-running workers.</li> </ul>"},{"location":"visibility_timeout/#example-worker-heartbeat","title":"Example: Worker Heartbeat","text":"<pre><code>msg = queue.read(\"my_queue\", vt=30)\nif msg:\n    try:\n        process_task(msg.message)\n        # Extend the lease while working\n        queue.set_vt(\"my_queue\", msg.msg_id, vt=60)\n        finalize_task(msg.message)\n        queue.archive(\"my_queue\", msg.msg_id)\n    except Exception:\n        # Let the message expire and become visible again\n        pass\n</code></pre>"}]}